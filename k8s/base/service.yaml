apiVersion: v1
kind: Service
metadata:
  name: civic-api
  namespace: platform
  labels:
    app: civic-api
spec:
  selector:
    app: civic-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
